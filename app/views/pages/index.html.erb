<% title h("Edit Site Content") %>

<h1><%= h yield(:title) %></h1>
<p>You can either edit the content here, or navigate to where the content is located and click the red edit icon. These bits of content can be formatted using the simple textile markup language.</p>

<table>
  <tr>
    <th>Name</th>
    <th>Permalink</th>
    <th>Content</th>
  </tr>
  <% for page in @pages %>
    <tr>
      <td><%=h page.name %></td>
      <td><%=h page.permalink %></td>
      <td><%=h truncate(page.content, :length => 260, :omission => "...(content continued)") %></td>
      <td><%= link_to "Show", page %></td>
      <td><%= link_to "Edit", edit_page_path(page) %></td>
      <td><%= link_to "Destroy", page, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>